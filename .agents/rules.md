# Antigravity Rules

- [x] Run commands automatically whenever reasonable, without asking for explicit permission each time.
- [ ] Maintain diagnostic logging in `Validator.tsx` and `ApiProxy.tsx` until the login issue is resolved.
